interface:
  display_name: "Image Watermark Batch"
  short_description: "Batch add transparent watermark overlays to image folders."
  default_prompt: "Add this PNG watermark to every image in the target folder, bottom-right with 20px padding, and append -ai generated to output filenames."
